# React Debug Test - Product Dashboard

## Setup

1. Create a Vite React + TS app
   npm create vite@latest

2. Install deps
   npm install

3. Replace App.tsx with given file

4. Add products.json in src/

5. Run
   npm run dev

## Task
Fix all bugs (11 total). Do not rewrite everything.
